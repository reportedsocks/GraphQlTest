query CharactersRequest($page: Int) {
    characters(page: $page) {
        info {
            next
        }
        results {
            id
            name
            image
            species
        }
    }
}